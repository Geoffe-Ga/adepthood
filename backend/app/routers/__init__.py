"""Route handlers for the FastAPI application."""

# This package collects individual router modules such as `practice` and
# `energy` to keep the `main` application file tidy and focused on
# composition.
